name: dataviewer

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14
          
      - name: Use local my-action
        uses: ./.github/actions/DayeLim.github.io

      - name: ls
        run: |
          ls
